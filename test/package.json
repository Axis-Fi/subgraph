{
  "name": "axisfi-auctions",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ axisfi-auctions",
    "create-local": "graph create --node http://localhost:8020/ axisfi-auctions",
    "remove-local": "graph remove --node http://localhost:8020/ axisfi-auctions",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 axisfi-auctions",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.67.4",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
